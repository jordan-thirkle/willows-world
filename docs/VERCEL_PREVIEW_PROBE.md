# Vercel preview semantics probe

Temporary no-runtime-change commit used only to verify that pull-request branches deploy to the Preview environment while `main` is the production branch.

This file can be removed after the deployment metadata is verified.
