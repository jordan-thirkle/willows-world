# P0 Slice 1 — Done State

The first implementation slice is complete when the existing Story Woods proving experience is driven by a validated repository-safe canonical Episode 001 record, negative revision/approval/rights tests pass, and relevant typecheck/build/browser regression evidence exists.

It is not complete merely because new interfaces/types were created. The existing experience must actually consume them through a clear adapter.
