# Implementation Command

After this branch is merged:

> Continue Willow's World from the earliest incomplete gate (P0). Inspect repository truth, create the focused implementation branch/worktree, execute `docs/NEXT-PR.md` test-first, verify with browser/build evidence, run the critic/review gate, fix valid findings, persist the PR/evidence, then continue to the next P0 slice while safe unblocked work remains. Do not restart planning or expand scope unless evidence requires it.
